// Web platform-specific empty polyfills file.
// On Web, all required APIs (Headers, FormData, fetch, AbortController)
// are natively supported by modern browsers, so no polyfills are needed.
export {};
